# The dev image builds from the repo root so it can install requirements.txt.
# Without this the build context is ~2.2 GB, nearly all of it pipeline output
# and the site, none of which the image needs.
data/
patchbook/
.git/
node_modules/
vendor/
_site/
__pycache__/
*.pyc
.env
